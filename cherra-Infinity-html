<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cherrak Infinity ♾️ - المنصة العالمية المتطورة</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-color: #1a237e;
            --secondary-color: #283593;
            --accent-color: #3949ab;
            --neon-blue: #00b4ff;
            --neon-purple: #b400ff;
            --neon-green: #00ff88;
            --neon-red: #ff4444;
            --neon-orange: #ffaa00;
            --neon-yellow: #ffff00;
            --light-color: #e8eaf6;
            --dark-color: #0d1533;
            --cosmic-color: #1a0033;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background: linear-gradient(135deg, var(--dark-color), var(--cosmic-color), #000);
            color: var(--light-color);
            min-height: 100vh;
            line-height: 1.6;
            overflow-x: hidden;
            position: relative;
        }

        /* الخلفية الكونية */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 20% 30%, rgba(120, 0, 255, 0.3) 0%, transparent 50%),
                radial-gradient(circle at 80% 70%, rgba(0, 180, 255, 0.2) 0%, transparent 50%),
                radial-gradient(circle at 40% 80%, rgba(255, 0, 150, 0.2) 0%, transparent 50%);
            z-index: -1;
            animation: cosmicGlow 8s ease-in-out infinite alternate;
        }

        @keyframes cosmicGlow {
            0% { opacity: 0.3; }
            100% { opacity: 0.7; }
        }
        
        .container {
            max-width: 1800px;
            margin: 0 auto;
            padding: 20px;
        }
        
        /* أنماط رأس الصفحة */
        header {
            text-align: center;
            padding: 40px 0;
            background: rgba(0, 0, 0, 0.7);
            border-radius: 25px;
            margin-bottom: 40px;
            box-shadow: 0 10px 40px rgba(0, 180, 255, 0.3);
            position: relative;
            overflow: hidden;
            border: 2px solid rgba(0, 180, 255, 0.3);
            backdrop-filter: blur(10px);
        }
        
        header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--neon-blue), var(--neon-purple), var(--neon-green));
            animation: headerGlow 3s infinite;
        }
        
        @keyframes headerGlow {
            0%, 100% { opacity: 0.7; }
            50% { opacity: 1; }
        }
        
        .logo {
            font-size: 6rem;
            margin-bottom: 20px;
            color: var(--light-color);
            text-shadow: 0 0 30px var(--neon-blue), 0 0 60px var(--neon-purple);
            animation: logoFloat 4s ease-in-out infinite;
        }
        
        @keyframes logoFloat {
            0%, 100% { transform: translateY(0) rotate(0deg); }
            50% { transform: translateY(-20px) rotate(5deg); }
        }
        
        h1 {
            font-size: 4rem;
            margin-bottom: 15px;
            background: linear-gradient(90deg, var(--neon-blue), var(--neon-purple), var(--neon-green));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-shadow: 0 0 20px rgba(0, 180, 255, 0.5);
        }
        
        .tagline {
            font-size: 1.5rem;
            opacity: 0.9;
            max-width: 1000px;
            margin: 0 auto;
            line-height: 1.8;
        }
        
        .stats {
            display: flex;
            justify-content: center;
            gap: 40px;
            margin-top: 30px;
            flex-wrap: wrap;
        }
        
        .stat-item {
            text-align: center;
            padding: 15px 25px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            border: 1px solid rgba(0, 180, 255, 0.3);
            backdrop-filter: blur(5px);
        }
        
        .stat-number {
            font-size: 2.5rem;
            font-weight: bold;
            color: var(--neon-green);
            text-shadow: 0 0 15px var(--neon-green);
        }
        
        .stat-label {
            font-size: 1.1rem;
            opacity: 0.8;
        }

        /* نظام بصمة الإصبع */
        .fingerprint-system {
            background: rgba(0, 0, 0, 0.7);
            padding: 40px;
            border-radius: 20px;
            margin: 40px 0;
            border: 2px solid rgba(0, 180, 255, 0.3);
            text-align: center;
            backdrop-filter: blur(10px);
        }

        .fingerprint-title {
            font-size: 2.5rem;
            margin-bottom: 30px;
            color: var(--neon-green);
            text-shadow: 0 0 20px var(--neon-green);
        }

        .fingerprint-scanner {
            width: 200px;
            height: 200px;
            margin: 0 auto 30px;
            border-radius: 50%;
            background: linear-gradient(135deg, #0a1a2a, #051423);
            border: 3px solid var(--neon-green);
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 0 30px rgba(0, 255, 136, 0.3);
        }

        .fingerprint-scanner:hover {
            transform: scale(1.05);
            box-shadow: 0 0 40px rgba(0, 255, 136, 0.5);
        }

        .fingerprint-scanner::before {
            content: '🫳';
            font-size: 4rem;
            animation: fingerprintPulse 2s ease-in-out infinite;
        }

        @keyframes fingerprintPulse {
            0%, 100% { transform: scale(1); opacity: 1; }
            50% { transform: scale(1.1); opacity: 0.8; }
        }

        .fingerprint-scanner.scanning::before {
            content: '🔍';
            animation: scanning 1s linear infinite;
        }

        @keyframes scanning {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .fingerprint-scanner.success::before {
            content: '✅';
            animation: none;
        }

        .fingerprint-status {
            font-size: 1.3rem;
            margin-top: 20px;
            color: var(--neon-blue);
            min-height: 60px;
        }

        /* نظام الأقمار الصناعية */
        .satellite-system {
            background: rgba(0, 0, 0, 0.7);
            padding: 30px;
            border-radius: 20px;
            margin: 30px 0;
            border: 2px solid rgba(255, 170, 0, 0.3);
            backdrop-filter: blur(10px);
        }

        .satellite-title {
            font-size: 2rem;
            margin-bottom: 20px;
            color: var(--neon-orange);
            text-shadow: 0 0 15px var(--neon-orange);
            text-align: center;
        }

        .satellite-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .satellite-item {
            background: rgba(255, 255, 255, 0.05);
            padding: 20px;
            border-radius: 15px;
            border: 1px solid rgba(255, 170, 0, 0.2);
            text-align: center;
        }

        .satellite-icon {
            font-size: 3rem;
            margin-bottom: 15px;
            color: var(--neon-orange);
        }

        .network-status {
            display: flex;
            justify-content: space-around;
            margin-top: 20px;
            flex-wrap: wrap;
        }

        .network-item {
            text-align: center;
            padding: 15px;
        }

        .network-speed {
            font-size: 1.5rem;
            font-weight: bold;
            color: var(--neon-green);
        }

        /* نظام الساعة الكونية */
        .cosmic-clock {
            background: rgba(0, 0, 0, 0.7);
            padding: 25px;
            border-radius: 20px;
            margin: 30px 0;
            border: 2px solid rgba(180, 0, 255, 0.3);
            text-align: center;
            backdrop-filter: blur(10px);
        }

        .clock-title {
            font-size: 2rem;
            margin-bottom: 20px;
            color: var(--neon-purple);
            text-shadow: 0 0 15px var(--neon-purple);
        }

        .clock-display {
            font-size: 3.5rem;
            font-weight: bold;
            color: var(--neon-blue);
            text-shadow: 0 0 20px var(--neon-blue);
            font-family: 'Courier New', monospace;
            margin: 20px 0;
        }

        .world-time {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-top: 20px;
        }

        .time-zone {
            background: rgba(255, 255, 255, 0.05);
            padding: 15px;
            border-radius: 10px;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        /* نظام الشحن الشمسي */
        .solar-charge {
            background: rgba(0, 0, 0, 0.7);
            padding: 30px;
            border-radius: 20px;
            margin: 30px 0;
            border: 2px solid rgba(255, 255, 0, 0.3);
            backdrop-filter: blur(10px);
        }

        .solar-title {
            font-size: 2rem;
            margin-bottom: 20px;
            color: var(--neon-yellow);
            text-shadow: 0 0 15px var(--neon-yellow);
            text-align: center;
        }

        .charge-status {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 30px;
            flex-wrap: wrap;
        }

        .battery {
            width: 120px;
            height: 60px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            border: 2px solid var(--neon-yellow);
            position: relative;
            overflow: hidden;
        }

        .battery-level {
            height: 100%;
            background: linear-gradient(90deg, var(--neon-yellow), var(--neon-orange));
            width: 75%;
            transition: width 2s ease;
        }

        .solar-icon {
            font-size: 4rem;
            animation: solarSpin 4s linear infinite;
        }

        @keyframes solarSpin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* نظام المكافآت */
        .reward-system {
            background: rgba(0, 0, 0, 0.7);
            padding: 30px;
            border-radius: 20px;
            margin: 30px 0;
            border: 2px solid rgba(0, 255, 136, 0.3);
            backdrop-filter: blur(10px);
        }

        .reward-title {
            font-size: 2rem;
            margin-bottom: 20px;
            color: var(--neon-green);
            text-shadow: 0 0 15px var(--neon-green);
            text-align: center;
        }

        .reward-stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .reward-item {
            background: rgba(255, 255, 255, 0.05);
            padding: 20px;
            border-radius: 15px;
            border: 1px solid rgba(0, 255, 136, 0.2);
            text-align: center;
        }

        .reward-amount {
            font-size: 2rem;
            font-weight: bold;
            color: var(--neon-green);
            margin: 10px 0;
        }

        /* نظام الحظر */
        .blocked-countries {
            background: rgba(0, 0, 0, 0.7);
            padding: 25px;
            border-radius: 20px;
            margin: 30px 0;
            border: 2px solid rgba(255, 0, 0, 0.3);
            backdrop-filter: blur(10px);
        }

        .blocked-title {
            font-size: 1.8rem;
            margin-bottom: 20px;
            color: var(--neon-red);
            text-shadow: 0 0 15px var(--neon-red);
            text-align: center;
        }

        .country-list {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 15px;
            margin-top: 20px;
        }

        .country-item {
            padding: 10px 20px;
            background: rgba(255, 0, 0, 0.1);
            border: 1px solid var(--neon-red);
            border-radius: 25px;
            color: var(--neon-red);
            font-weight: bold;
        }

        .country-item.allowed {
            background: rgba(0, 255, 136, 0.1);
            border: 1px solid var(--neon-green);
            color: var(--neon-green);
        }

        /* تأثيرات النجوم */
        .stars {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: -1;
        }

        .star {
            position: absolute;
            background: white;
            border-radius: 50%;
            animation: twinkle 3s infinite;
        }

        @keyframes twinkle {
            0%, 100% { opacity: 0.3; }
            50% { opacity: 1; }
        }

        /* أنماط الاستجابة */
        @media (max-width: 1200px) {
            h1 { font-size: 3rem; }
            .logo { font-size: 5rem; }
        }

        @media (max-width: 768px) {
            h1 { font-size: 2.5rem; }
            .logo { font-size: 4rem; }
            .clock-display { font-size: 2.5rem; }
        }

        /* أنماط البوابة الرئيسية */
        .main-gate {
            background: rgba(0, 0, 0, 0.8);
            padding: 50px;
            border-radius: 25px;
            margin: 40px 0;
            border: 3px solid rgba(0, 180, 255, 0.4);
            text-align: center;
            backdrop-filter: blur(15px);
        }

        .gate-title {
            font-size: 3rem;
            margin-bottom: 30px;
            color: var(--neon-blue);
            text-shadow: 0 0 25px var(--neon-blue);
        }

        .gate-content {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            margin-top: 40px;
        }

        .gate-feature {
            background: rgba(255, 255, 255, 0.05);
            padding: 25px;
            border-radius: 15px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            text-align: center;
        }

        .feature-icon {
            font-size: 4rem;
            margin-bottom: 20px;
        }

        .feature-title {
            font-size: 1.5rem;
            margin-bottom: 15px;
            color: var(--neon-green);
        }

        /* لوحة التحكم الجديدة */
        .dashboard {
            background: rgba(0, 0, 0, 0.8);
            padding: 40px;
            border-radius: 25px;
            margin: 40px 0;
            border: 3px solid rgba(180, 0, 255, 0.4);
            backdrop-filter: blur(15px);
            display: none;
        }

        .dashboard-title {
            font-size: 2.5rem;
            margin-bottom: 30px;
            color: var(--neon-purple);
            text-shadow: 0 0 20px var(--neon-purple);
            text-align: center;
        }

        .dashboard-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 25px;
            margin-top: 30px;
        }

        .dashboard-card {
            background: rgba(255, 255, 255, 0.05);
            padding: 25px;
            border-radius: 15px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            text-align: center;
            transition: transform 0.3s ease;
        }

        .dashboard-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        }

        .card-icon {
            font-size: 3.5rem;
            margin-bottom: 15px;
        }

        .card-title {
            font-size: 1.5rem;
            margin-bottom: 15px;
            color: var(--neon-blue);
        }

        .card-value {
            font-size: 2rem;
            font-weight: bold;
            color: var(--neon-green);
            margin: 10px 0;
        }

        /* نظام الإشعارات */
        .notifications {
            position: fixed;
            top: 20px;
            left: 20px;
            z-index: 1000;
        }

        .notification {
            background: rgba(0, 0, 0, 0.8);
            color: white;
            padding: 15px 25px;
            border-radius: 10px;
            margin-bottom: 10px;
            border-left: 4px solid var(--neon-green);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
            transform: translateX(-100%);
            transition: transform 0.3s ease;
            backdrop-filter: blur(10px);
        }

        .notification.show {
            transform: translateX(0);
        }

        .notification.error {
            border-left-color: var(--neon-red);
        }

        .notification.warning {
            border-left-color: var(--neon-orange);
        }

        /* نظام التقدم */
        .progress-bar {
            width: 100%;
            height: 10px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 5px;
            margin: 15px 0;
            overflow: hidden;
        }

        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, var(--neon-blue), var(--neon-purple));
            border-radius: 5px;
            transition: width 0.5s ease;
        }

        /* أزرار متطورة */
        .btn-advanced {
            background: linear-gradient(45deg, var(--neon-blue), var(--neon-purple));
            color: white;
            border: none;
            padding: 12px 25px;
            border-radius: 25px;
            font-size: 1.1rem;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }

        .btn-advanced:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
        }

        .btn-advanced:active {
            transform: translateY(0);
        }
    </style>
</head>
<body>
    <!-- تأثير النجوم -->
    <div class="stars" id="starsContainer"></div>

    <!-- نظام الإشعارات -->
    <div class="notifications" id="notifications"></div>

    <div class="container">
        <header>
            <div class="logo">🧲</div>
            <h1>Cherrak Infinity ♾️</h1>
            <p class="tagline">المنصة العالمية المتطورة - نظام الدخول ببصمة الإصبع 🫳 والأقمار الصناعية الذكية 🛰</p>
            
            <div class="stats">
                <div class="stat-item">
                    <div class="stat-number" id="onlineUsers">12,847</div>
                    <div class="stat-label">مستخدمون متصلون</div>
                </div>
                <div class="stat-item">
                    <div class="stat-number" id="totalEarnings">$127,430</div>
                    <div class="stat-label">أرباح المستخدمين</div>
                </div>
                <div class="stat-item">
                    <div class="stat-number" id="networkSpeed">3.8 G</div>
                    <div class="stat-label">سرعة الشبكة</div>
                </div>
                <div class="stat-item">
                    <div class="stat-number">∞</div>
                    <div class="stat-label">إمكانيات لا نهائية</div>
                </div>
            </div>
        </header>

        <!-- البوابة الرئيسية -->
        <section class="main-gate" id="mainGate">
            <h2 class="gate-title">🚪 بوابة Cherrak Infinity الكونية</h2>
            <p style="font-size: 1.3rem; margin-bottom: 30px; opacity: 0.9;">
                اختر طريقة الدخول إلى المنصة المتطورة
            </p>
            
            <div class="gate-content">
                <div class="gate-feature">
                    <div class="feature-icon">🫳</div>
                    <h3 class="feature-title">بصمة الإصبع</h3>
                    <p>دخول آبي باستخدام البصمة البيومترية</p>
                    <button onclick="showFingerprintScanner()" class="btn-advanced">
                        تفعيل البصمة
                    </button>
                </div>
                
                <div class="gate-feature">
                    <div class="feature-icon">🔑</div>
                    <h3 class="feature-title">كلمة المرور</h3>
                    <p>الدخول التقليدي بكلمة المرور</p>
                    <button onclick="showPasswordLogin()" class="btn-advanced">
                        الدخول بكلمة المرور
                    </button>
                </div>
                
                <div class="gate-feature">
                    <div class="feature-icon">👁️</div>
                    <h3 class="feature-title">التعرف على الوجه</h3>
                    <p>الدخول باستخدام تقنية التعرف على الوجه</p>
                    <button onclick="showFaceRecognition()" class="btn-advanced">
                        تفعيل التعرف على الوجه
                    </button>
                </div>
            </div>
        </section>

        <!-- لوحة التحكم المتقدمة -->
        <section class="dashboard" id="dashboard">
            <h2 class="dashboard-title">🎛️ لوحة تحكم Cherrak Infinity المتقدمة</h2>
            
            <div class="dashboard-grid">
                <div class="dashboard-card">
                    <div class="card-icon">📊</div>
                    <h3 class="card-title">تحليلات الأداء</h3>
                    <div class="card-value" id="performanceScore">98%</div>
                    <div class="progress-bar">
                        <div class="progress-fill" style="width: 98%"></div>
                    </div>
                    <p>أداء النظام العام</p>
                </div>
                
                <div class="dashboard-card">
                    <div class="card-icon">🔒</div>
                    <h3 class="card-title">الأمان</h3>
                    <div class="card-value" id="securityLevel">100%</div>
                    <div class="progress-bar">
                        <div class="progress-fill" style="width: 100%"></div>
                    </div>
                    <p>مستوى حماية المنصة</p>
                </div>
                
                <div class="dashboard-card">
                    <div class="card-icon">🚀</div>
                    <h3 class="card-title">السرعة</h3>
                    <div class="card-value" id="speedIndex">3.8G</div>
                    <div class="progress-bar">
                        <div class="progress-fill" style="width: 95%"></div>
                    </div>
                    <p>سرعة الشبكة الحالية</p>
                </div>
                
                <div class="dashboard-card">
                    <div class="card-icon">💰</div>
                    <h3 class="card-title">الأرباح</h3>
                    <div class="card-value" id="userProfit">$861</div>
                    <div class="progress-bar">
                        <div class="progress-fill" style="width: 65%"></div>
                    </div>
                    <p>إجمالي أرباحك</p>
                </div>
                
                <div class="dashboard-card">
                    <div class="card-icon">👥</div>
                    <h3 class="card-title">الفريق</h3>
                    <div class="card-value" id="teamMembers">287</div>
                    <div class="progress-bar">
                        <div class="progress-fill" style="width: 28%"></div>
                    </div>
                    <p>أعضاء فريقك</p>
                </div>
                
                <div class="dashboard-card">
                    <div class="card-icon">🎯</div>
                    <h3 class="card-title">الأهداف</h3>
                    <div class="card-value" id="goalsProgress">75%</div>
                    <div class="progress-bar">
                        <div class="progress-fill" style="width: 75%"></div>
                    </div>
                    <p>التقدم نحو الهدف</p>
                </div>
            </div>
            
            <div style="text-align: center; margin-top: 40px;">
                <button onclick="showMainGate()" class="btn-advanced" style="background: linear-gradient(45deg, var(--neon-red), var(--neon-orange));">
                    🚪 العودة إلى البوابة الرئيسية
                </button>
            </div>
        </section>

        <!-- نظام بصمة الإصبع -->
        <section class="fingerprint-system" id="fingerprintSection" style="display: none;">
            <h2 class="fingerprint-title">🫳 نظام الدخول ببصمة الإصبع</h2>
            <div class="fingerprint-scanner" id="fingerprintScanner">
                <!-- بصمة الإصبع تظهر هنا -->
            </div>
            <div class="fingerprint-status" id="fingerprintStatus">
                اضغط على الماسح الضوئي للدخول إلى المنصة
            </div>
            <button onclick="hideFingerprintScanner()" class="btn-advanced" style="background: transparent; border: 2px solid var(--neon-blue);">
                الرجوع إلى البوابة الرئيسية
            </button>
        </section>

        <!-- نظام الأقمار الصناعية -->
        <section class="satellite-system">
            <h2 class="satellite-title">🛰 نظام الأقمار الصناعية الذكي</h2>
            <div class="satellite-grid">
                <div class="satellite-item">
                    <div class="satellite-icon">🛰</div>
                    <h3>قمر 5G المتقدم</h3>
                    <p>تعزيز سرعة الإنترنت وتغطية 5G</p>
                    <div style="margin-top: 10px; color: var(--neon-green); font-weight: bold;">🟢 نشط</div>
                </div>
                <div class="satellite-item">
                    <div class="satellite-icon">🚀</div>
                    <h3>قمر 6G التجريبي</h3>
                    <p>أحدث تقنيات الجيل السادس</p>
                    <div style="margin-top: 10px; color: var(--neon-orange); font-weight: bold;">🟡 تجريبي</div>
                </div>
                <div class="satellite-item">
                    <div class="satellite-icon">🤖</div>
                    <h3>الذكاء الاصطناعي</h3>
                    <p>تحسين الأداء باستخدام AI</p>
                    <div style="margin-top: 10px; color: var(--neon-green); font-weight: bold;">🟢 نشط</div>
                </div>
            </div>
            <div class="network-status">
                <div class="network-item">
                    <div class="network-speed" id="speed5g">856 Mbps</div>
                    <div>سرعة 5G</div>
                </div>
                <div class="network-item">
                    <div class="network-speed" id="speed6g">3.2 Gbps</div>
                    <div>سرعة 6G</div>
                </div>
                <div class="network-item">
                    <div class="network-speed" id="latency">9.2 ms</div>
                    <div>زمن الاستجابة</div>
                </div>
            </div>
        </section>

        <!-- الساعة الكونية -->
        <section class="cosmic-clock">
            <h2 class="clock-title">⏰ الساعة الكونية العالمية</h2>
            <div class="clock-display" id="cosmicClock">
                00:00:00
            </div>
            <div class="world-time">
                <div class="time-zone">
                    <div>🇩🇿 الجزائر</div>
                    <div id="timeAlgeria">00:00:00</div>
                </div>
                <div class="time-zone">
                    <div>🇸🇦 السعودية</div>
                    <div id="timeKSA">00:00:00</div>
                </div>
                <div class="time-zone">
                    <div>🇦🇪 الإمارات</div>
                    <div id="timeUAE">00:00:00</div>
                </div>
                <div class="time-zone">
                    <div>🌍 UTC</div>
                    <div id="timeUTC">00:00:00</div>
                </div>
            </div>
        </section>

        <!-- نظام الشحن الشمسي -->
        <section class="solar-charge">
            <h2 class="solar-title">🌞 نظام الشحن بالطاقة الشمسية</h2>
            <div class="charge-status">
                <div class="solar-icon">☀️</div>
                <div class="battery">
                    <div class="battery-level" id="batteryLevel"></div>
                </div>
                <div>
                    <div id="chargeStatus">جاري الشحن...</div>
                    <div id="chargePercentage" style="font-size: 1.5rem; font-weight: bold; color: var(--neon-yellow);">75%</div>
                </div>
            </div>
        </section>

        <!-- نظام المكافآت -->
        <section class="reward-system">
            <h2 class="reward-title">🏆 نظام المكافآت والدعوة</h2>
            <div class="reward-stats">
                <div class="reward-item">
                    <div>المستخدمين المدعوين</div>
                    <div class="reward-amount" id="invitedUsers">287</div>
                    <div>مستخدم</div>
                </div>
                <div class="reward-item">
                    <div>الأرباح المحققة</div>
                    <div class="reward-amount" id="userEarnings">$861</div>
                    <div>دولار</div>
                </div>
                <div class="reward-item">
                    <div>المكافأة القادمة</div>
                    <div class="reward-amount" id="nextReward">$3</div>
                    <div>عند 1000 مستخدم</div>
                </div>
            </div>
            <div style="text-align: center; margin-top: 25px;">
                <button class="btn-advanced" onclick="showNotification('🎁 تم نسخ رابط الدعوة! شاركه مع أصدقائك الآن.', 'success')">
                    🎁 ادعُ أصدقاءك واكسب $3 لكل 1000 مستخدم
                </button>
            </div>
        </section>

        <!-- نظام الحظر -->
        <section class="blocked-countries">
            <h2 class="blocked-title">🚫 الدول المحظورة من الدخول</h2>
            <div class="country-list">
                <div class="country-item">🇮🇱 إسرائيل</div>
                <div class="country-item allowed">🇩🇿 الجزائر</div>
                <div class="country-item allowed">🇸🇦 السعودية</div>
                <div class="country-item allowed">🇦🇪 الإمارات</div>
                <div class="country-item allowed">🇪🇬 مصر</div>
                <div class="country-item allowed">🇶🇦 قطر</div>
                <div class="country-item allowed">🇰🇼 الكويت</div>
                <div class="country-item allowed">🇴🇲 عمان</div>
                <div class="country-item allowed">🇹🇳 تونس</div>
                <div class="country-item allowed">🇲🇦 المغرب</div>
            </div>
        </section>

        <!-- باقي المحتوى -->
        <footer style="text-align: center; padding: 40px 0; margin-top: 50px; border-top: 2px solid rgba(255,255,255,0.1);">
            <p>Cherrak Infinity ♾️ - المنصة العالمية المتطورة - جميع الحقوق محفوظة © 2024</p>
            <p>نظام الدخول ببصمة الإصبع 🫳 | الأقمار الصناعية الذكية 🛰 | الطاقة الشمسية 🌞</p>
            <p style="margin-top: 15px; font-size: 0.9rem; opacity: 0.7;">
                ⚠️ ملاحظة: هذا نموذج محاكاة للعرض. بعض الميزات تتطلب بيئة حقيقية لتعمل كاملاً.
            </p>
        </footer>
    </div>

    <script>
        // إنشاء النجوم
        function createStars() {
            const starsContainer = document.getElementById('starsContainer');
            for (let i = 0; i < 200; i++) {
                const star = document.createElement('div');
                star.className = 'star';
                star.style.width = Math.random() * 3 + 'px';
                star.style.height = star.style.width;
                star.style.left = Math.random() * 100 + '%';
                star.style.top = Math.random() * 100 + '%';
                star.style.animationDelay = Math.random() * 3 + 's';
                starsContainer.appendChild(star);
            }
        }

        // نظام الإشعارات
        function showNotification(message, type = 'success') {
            const notifications = document.getElementById('notifications');
            const notification = document.createElement('div');
            notification.className = `notification ${type}`;
            notification.textContent = message;
            notifications.appendChild(notification);
            
            // إظهار الإشعار
            setTimeout(() => {
                notification.classList.add('show');
            }, 100);
            
            // إخفاء الإشعار بعد 5 ثوان
            setTimeout(() => {
                notification.classList.remove('show');
                setTimeout(() => {
                    notifications.removeChild(notification);
                }, 300);
            }, 5000);
        }

        // نظام بصمة الإصبع
        function showFingerprintScanner() {
            document.getElementById('fingerprintSection').style.display = 'block';
            document.getElementById('mainGate').style.display = 'none';
        }

        function hideFingerprintScanner() {
            document.getElementById('fingerprintSection').style.display = 'none';
            document.getElementById('mainGate').style.display = 'block';
            resetFingerprintScanner();
        }

        function resetFingerprintScanner() {
            const scanner = document.getElementById('fingerprintScanner');
            const status = document.getElementById('fingerprintStatus');
            
            scanner.classList.remove('scanning', 'success');
            scanner.style.transform = 'scale(1)';
            status.textContent = 'اضغط على الماسح الضوئي للدخول إلى المنصة';
            status.style.color = 'var(--neon-blue)';
        }

        const fingerprintScanner = document.getElementById('fingerprintScanner');
        const fingerprintStatus = document.getElementById('fingerprintStatus');

        fingerprintScanner.addEventListener('click', function() {
            if (this.classList.contains('success')) return;
            
            this.classList.add('scanning');
            fingerprintStatus.textContent = '🔍 جاري مسح البصمة...';
            fingerprintStatus.style.color = 'var(--neon-orange)';
            
            // محاكاة المسح
            setTimeout(() => {
                this.classList.remove('scanning');
                this.classList.add('success');
                fingerprintStatus.innerHTML = '✅ تم التعرف على البصمة بنجاح!<br>جاري الدخول إلى المنصة...';
                fingerprintStatus.style.color = 'var(--neon-green)';
                
                // محاكاة الدخول إلى المنصة
                setTimeout(() => {
                    showNotification('🎉 تم الدخول بنجاح باستخدام بصمة الإصبع!', 'success');
                    showDashboard();
                }, 2000);
            }, 3000);
        });

        function showPasswordLogin() {
            const password = prompt('🔐 أدخل كلمة المرور للدخول:', 'Bismillah');
            if (password === 'Bismillah') {
                showNotification('🎉 تم الدخول بنجاح باستخدام كلمة المرور!', 'success');
                showDashboard();
            } else {
                showNotification('❌ كلمة المرور غير صحيحة', 'error');
            }
        }

        function showFaceRecognition() {
            showNotification('👁️ نظام التعرف على الوجه قيد التطوير', 'warning');
        }

        function showDashboard() {
            document.getElementById('mainGate').style.display = 'none';
            document.getElementById('dashboard').style.display = 'block';
            document.getElementById('fingerprintSection').style.display = 'none';
            
            // عرض إشعار ترحيبي
            setTimeout(() => {
                showNotification('🚀 مرحبًا في لوحة تحكم Cherrak Infinity! تم تفعيل جميع الأنظمة.', 'success');
            }, 500);
        }

        function showMainGate() {
            document.getElementById('dashboard').style.display = 'none';
            document.getElementById('mainGate').style.display = 'block';
        }

        // نظام الساعة الكونية
        function updateCosmicClock() {
            const now = new Date();
            const cosmicClock = document.getElementById('cosmicClock');
            const timeAlgeria = document.getElementById('timeAlgeria');
            const timeKSA = document.getElementById('timeKSA');
            const timeUAE = document.getElementById('timeUAE');
            const timeUTC = document.getElementById('timeUTC');

            cosmicClock.textContent = now.toLocaleTimeString('ar-EG');
            
            // أوقات الدول
            const options = { timeZone: 'Africa/Algiers', hour12: true, hour: '2-digit', minute: '2-digit', second: '2-digit' };
            timeAlgeria.textContent = now.toLocaleTimeString('ar-EG', options);
            
            options.timeZone = 'Asia/Riyadh';
            timeKSA.textContent = now.toLocaleTimeString('ar-EG', options);
            
            options.timeZone = 'Asia/Dubai';
            timeUAE.textContent = now.toLocaleTimeString('ar-EG', options);
            
            options.timeZone = 'UTC';
            timeUTC.textContent = now.toLocaleTimeString('ar-EG', options);
        }

        // نظام الأقمار الصناعية
        function updateNetworkSpeeds() {
            const speed5g = document.getElementById('speed5g');
            const speed6g = document.getElementById('speed6g');
            const latency = document.getElementById('latency');
            const networkSpeed = document.getElementById('networkSpeed');
            const speedIndex = document.getElementById('speedIndex');

            // محاكاة سرعات عشوائية واقعية
            const speed5gValue = 850 + Math.random() * 150;
            const speed6gValue = 3200 + Math.random() * 800;
            const latencyValue = 8 + Math.random() * 4;

            speed5g.textContent = Math.round(speed5gValue) + ' Mbps';
            speed6g.textContent = (speed6gValue / 1000).toFixed(1) + ' Gbps';
            latency.textContent = latencyValue.toFixed(1) + ' ms';
            networkSpeed.textContent = (speed6gValue / 1000).toFixed(1) + ' G';
            speedIndex.textContent = (speed6gValue / 1000).toFixed(1) + 'G';
        }

        // نظام الشحن الشمسي
        function updateBatteryStatus() {
            const batteryLevel = document.getElementById('batteryLevel');
            const chargeStatus = document.getElementById('chargeStatus');
            const chargePercentage = document.getElementById('chargePercentage');

            // محاكاة حالة الشحن
            const chargeLevel = 75 + Math.random() * 10;
            batteryLevel.style.width = chargeLevel + '%';
            chargePercentage.textContent = Math.round(chargeLevel) + '%';

            if (chargeLevel > 90) {
                chargeStatus.textContent = '🔋 مشحون بالكامل';
                chargeStatus.style.color = 'var(--neon-green)';
            } else {
                chargeStatus.textContent = '☀️ جاري الشحن بالطاقة الشمسية';
                chargeStatus.style.color = 'var(--neon-yellow)';
            }
        }

        // نظام الإحصائيات
        function updateStats() {
            const onlineUsers = document.getElementById('onlineUsers');
            const totalEarnings = document.getElementById('totalEarnings');
            const invitedUsers = document.getElementById('invitedUsers');
            const userEarnings = document.getElementById('userEarnings');
            const userProfit = document.getElementById('userProfit');
            const teamMembers = document.getElementById('teamMembers');

            // محاكاة بيانات حية
            const onlineChange = Math.random() * 50 - 25;
            const newOnline = Math.max(10000, 12847 + onlineChange);
            onlineUsers.textContent = Math.round(newOnline).toLocaleString();
            
            const earningsChange = Math.random() * 1000 - 500;
            const newEarnings = Math.max(100000, 127430 + earningsChange);
            totalEarnings.textContent = '$' + Math.round(newEarnings).toLocaleString();
            
            const invitedChange = Math.random() * 10 - 5;
            const newInvited = Math.max(200, 287 + invitedChange);
            invitedUsers.textContent = Math.round(newInvited).toLocaleString();
            
            userEarnings.textContent = '$' + Math.round(newInvited * 3).toLocaleString();
            userProfit.textContent = '$' + Math.round(newInvited * 3).toLocaleString();
            teamMembers.textContent = Math.round(newInvited).toLocaleString();
        }

        // تحديث لوحة التحكم
        function updateDashboard() {
            const performanceScore = document.getElementById('performanceScore');
            const goalsProgress = document.getElementById('goalsProgress');
            
            // محاكاة تغييرات عشوائية في لوحة التحكم
            const performanceChange = Math.random() * 4 - 2;
            const newPerformance = Math.max(90, Math.min(100, 98 + performanceChange));
            performanceScore.textContent = Math.round(newPerformance) + '%';
            document.querySelector('#performanceScore + .progress-bar .progress-fill').style.width = Math.round(newPerformance) + '%';
            
            const goalsChange = Math.random() * 10 - 5;
            const newGoals = Math.max(50, Math.min(100, 75 + goalsChange));
            goalsProgress.textContent = Math.round(newGoals) + '%';
            document.querySelector('#goalsProgress + .progress-bar .progress-fill').style.width = Math.round(newGoals) + '%';
        }

        // التهيئة
        document.addEventListener('DOMContentLoaded', function() {
            createStars();
            updateCosmicClock();
            updateNetworkSpeeds();
            updateBatteryStatus();
            updateStats();
            updateDashboard();

            // تحديث البيانات كل ثانية
            setInterval(updateCosmicClock, 1000);
            setInterval(updateNetworkSpeeds, 3000);
            setInterval(updateBatteryStatus, 5000);
            setInterval(updateStats, 10000);
            setInterval(updateDashboard, 8000);
            
            // عرض إشعار ترحيبي عند التحميل
            setTimeout(() => {
                showNotification('✨ مرحبًا بكم في Cherrak Infinity - المنصة العالمية المتطورة', 'success');
            }, 1000);
        });

        // التأكد من حظر إسرائيل
        function checkCountryAccess() {
            // في التطبيق الحقيقي، هذا سيتحقق من IP المستخدم
            const userCountry = 'DZ'; // محاكاة - الجزائر
            if (userCountry === 'IL') {
                showNotification('🚫 هذا البلد محظور من الدخول', 'error');
                return false;
            }
            return true;
        }

        // التحقق عند التحميل
        checkCountryAccess();
    </script>
</body>
</html>
