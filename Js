
// مثال مبسط لتسجيل الدخول
function login() {
  const password = document.getElementById("password").value;
  if (password === "Bismillah") {
    window.location.href = "library.html"; // يفتح المكتبة الشاملة
  } else {
    alert("كلمة المرور غير صحيحة");
  }
}
